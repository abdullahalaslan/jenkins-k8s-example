pipeline {
  stages {
    stage('Deploy App') {
      steps {
        script {
          kubernetesDeploy(configs: "hellodocker.yaml", kubeconfigId: "MINIKUBECONFIG")
        }
      }
    }
  }
}
